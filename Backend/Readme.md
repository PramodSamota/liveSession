# 🎥 Live Session Backend (Node.js + MongoDB)

## 📘 Overview

This is the backend API for the **Live Session Web App**.  
It allows an admin to start a live session and generates a unique session link for students.  
Both admin and students can access the same video session using the same unique ID.

---

## 🧱 Tech Stack

- **Node.js** + **Express.js** — RESTful API
- **MongoDB** + **Mongoose** — Database
- **UUID** — Generate unique session IDs
- **dotenv** — Manage environment variables
- **CORS** — Allow cross-origin access

---

## ⚙️ Setup Instructions

### 1️⃣ Clone the Repository

```bash
git clone https://github.com/yourusername/live-session-app.git
cd live-session-app/Backend
```
